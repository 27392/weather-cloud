server:
  port: 8670
spring:
  application:
    name: report-service
  thymeleaf:
    cache: false
    encoding: UTF-8
    suffix: .html
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
feign:
  hystrix:
    enabled: true